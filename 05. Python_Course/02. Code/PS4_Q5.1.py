# Import the math module and use it to:
# Find the square root of 144
# Calculate sin(90°) (hint: use math.radians() )
# Solution ==>

import math as m
import os as o

print(m.sqrt(144))
print(round((m.sin(m.radians(30))) , 1))